export default function getModelNumber() {
  // eslint-disable-next-line no-unused-vars
  const modelRegex = new RegExp(/\b((?=[A-Za-z/ -]{0,19}\d)[A-Za-z0-9/ -]{4,20})\b/);
  // TODO: write DOM-scraper function to get product model number
}
