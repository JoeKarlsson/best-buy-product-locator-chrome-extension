import React from 'react';
import styled from 'styled-components';

const ImageContainer = styled.div`
  text-align: center;
`;

const Logo = () => <ImageContainer>Loading</ImageContainer>;

export default Logo;
